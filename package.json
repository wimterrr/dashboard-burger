{
  "name": "dashboard-burger",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/build-board.mjs && node scripts/sync-pages.mjs",
    "preview": "python3 -m http.server 4174 -d dist"
  }
}
